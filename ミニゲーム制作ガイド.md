# ミニゲーム制作ガイド

`授業制作ガイド.md`で作る本編（学習ゲーム型授業）とは別に、**特定の単元をクリアした子への「ごほうびミニゲーム」**を作るための手引き。星つなぎタイムアタック（hoshitsunagi、星めぐりの塔クリア後に解放）で確立。

技術的な土台（音・canvas・キラキラ演出などのコード資産）は `授業制作ガイド.md` と完全に共通。このガイドは「授業フォーマットを外す・アンロックを付ける」という**差分だけ**を定義する。

---

## 0. 位置づけ・品質基準

- `授業制作ガイド.md` 2章の5つ星フォーマット（intro→vote→ミッション×4→はかせチャレンジ→ふりかえりタイム→ending→free）は**使わない**。完全にゲームとして完結させる
- 特定の本編作品をクリアした子だけが遊べる**アンロック式ごほうび**として作る（本編と同列の「学習カード」にはしない）
- 品質基準は授業制作ガイド0章と同じ「夢中になる」体験だが、ここでの主目的は**定着・再訪動機**（学びそのものは本編で既に済んでいる前提）。vote/はかせチャレンジ/ending穴埋めのような教え直しの要素は入れない

---

## 1. 着手前に決めること（授業制作ガイド1章に相当）

1. **元になる本編作品**（アンロック条件にする単元・slug）
2. **コアループ**（操作して何を目指すか、勝敗/クリア条件）。**既存作と入力方式が重複しないか確認する**——「ダイヤル選択」「押しっぱなしキープ」「連続タップで正順選択」「連打/マッシュ」など、シリーズ全体の一覧は`project-minigame-series.md`（memory）で管理されている。入力パターンを使い回さないことが「1つずつのクオリティ」を保つ鍵
3. **難易度階段**（レベル制/タイムアタック等の進行）。可能な限り、本編の実験結果・観察コメントにある具体的な数値（例：「手回し発電機は1秒に2〜3回が目安」）を難易度設計のアンカーにする。裏付けのない架空の目安値は作らない
4. **素材の本物度**：本編で使った数値・用語・実在の名称をどこまで正確に引き継ぐか。授業制作ガイド5章「数値・記号・用語は本物」の原則をそのまま踏襲する（例：星座名・恒星名・等級・星座線の伝統的なつなぎ方は実在のものを正確に使う）
5. **数値バランスの事前検証**（連打・キープ・耐久など、プレイヤーの入力頻度や継続時間が勝敗を左右するコアループの場合は必須）：実装前にNode.jsで簡易シミュレーションを書き、①一定間隔で入力し続けた場合の定常状態、②人間らしいゆらぎ（±30%程度のジッター）を入れた場合、の両方で各レベルが対象年齢の子が現実的に出せる入力速度でクリア可能か検証する。実装後の手戻り（みゃくキープでバランス崩壊を実装後に発見した例）を防ぐため、実装前の工程として組み込む
6. 合意した設計方針を **memory に書き残す**（授業制作ガイド1章6と同じ運用。`project-minigame-series.md`に追記する形でよい）

---

## 1.5 実装が未完了のまま会話が終わる場合（作業再開の引き継ぎ）

対象本編・コアループの合意から実装・QA完了までを1回の会話で終えられず、途中で会話が終わる（ユーザーが`/clear`する等）ことがある。授業制作ガイド側の「実装が未完了のまま会話が終わる場合」と同じ考え方をミニゲームにも適用する：

- コアループが確定した時点、および区切りのよいタイミング（数値バランス検証完了時、実装完了時、QA完了時など）で、`project-minigame-series.md`の該当作品セクション（無ければ新規作成）を更新する。セクション見出しに**「（実装中）」**を付け、内容は最低限：①対象本編・タイトル・ファイル名（slug）、②確定済みの設計（コアループ・入力パターン・難易度階段・主要な数値と出典）、③現在の進捗（どこまで実装・QA済みか）、④次にやること、を書く。
- `MEMORY.md`のミニゲームシリーズ索引行にも、進捗が分かる一言（例：「最新は◯◯(slug)実装中、△△まで完了」）を反映する。
- 作品が完成したら、通常どおり「7. 完成後にやること」チェックリストを実行し、この進行中セクションを完成版の記録に書き換える（見出しの「（実装中）」を削除し、実装・QAの詳細を書き足す）。新しいセクションを別に作る必要はない。
- 新しい会話でミニゲーム制作を再開する時は、`project-minigame-series.md`に「（実装中）」の見出しが無いか先に確認する。見つかったら、対象本編・コアループの選定をやり直さず、そのメモの「次にやること」からそのまま続きに着手する（前回合意内容が古くなっていないか一言確認する程度でよい）。

---

## 2. アンロック機構（授業にはない新規パターン、必ず入れる）

各授業HTMLは `progress.js` の `recordBadge(count, total)` 経由で、`elementgirl_badge_progress`（`profile.js` の `profileNamespacedKey()` でプロフィール別に名前空間化されたキー）に slug 単位の進捗 `{count, total, updatedAt}` を書き込んでいる（index.html の `isCleared()` と同じデータ）。

ミニゲーム側は同じキーを読み、対象slugが `count > 0 && total > 0 && count >= total` なら解放と判定する：

```js
function getBadgeProgress(){
  try {
    var key = typeof profileNamespacedKey === "function"
      ? profileNamespacedKey("elementgirl_badge_progress") : "elementgirl_badge_progress";
    return JSON.parse(localStorage.getItem(key) || "{}");
  } catch(e){ return {}; }
}
function isUnlocked(slug){
  var p = getBadgeProgress()[slug];
  return !!(p && p.total > 0 && p.count >= p.total);
}
```

- 未クリア時は**ロック画面**を表示し、対象の本編作品へのリンクを示す（「まずは⭐⭐⭐⭐⭐ ○○をクリアしてね」）
- `auth.js`・`orientation-guard.js`・`profile.js`・`progress.js` は他作品と同じ4本を `<head>` でそのまま読み込む（`progress.js` はミニゲーム自体にはバッジが無いため実質使わないが、他作品との読み込み構成を揃えるため残す）
- ベストスコア/ベストタイムも同じ `profileNamespacedKey()` パターンで別キーに保存し、プロフィールごとに記録を分ける

---

## 3. 画面骨格（授業制作ガイド3章とは別パターン）

```
┌─ header：ロゴ ＋ タイマー/レベル表示 ──────────────┐
├─ main：単一の大きなステージ canvas（操作の主舞台）──┤
│    タップ/クリックで進行                            │
└─ リザルトオーバーレイ（クリア時のみ）────────────────┘
```

- 丸窓・案内役キャラ・おねがいカード・選択肢ボタン列・せんせい用バー・`?step=` 途中復帰は**不要**（授業ではないため）
- 背景の夜空演出（`#starsLayer` の twinkle アニメ等、本編と共通のテーマ色）は世界観の統一感のため流用してよい

---

## 4. 技術資産（授業制作ガイド6章をそのまま流用、新規に書き直さない）

- **音**：`tone()` + `ac()`、効果音（sTap/sYes/sFanfare 等）
- **canvas**：`fitCanvas()`（dpr対応）／`mapTransform()`（`PAD_T` で上部帯を確保）
- **キラキラ**：`burst()` / `drawFx()`（星形パーティクル）
- **CSS**：`clamp()` と vh/vw でレスポンシブ
- コピー元は対象の本編作品にする（同じ世界観の星空背景・星の描画関数をそのまま使い回せる）

---

## 4.5 実装時の定番の落とし穴（クオリティを落とす既知パターン）

- **rubyタグの自己参照・空rtタグ**：固有名詞（ミニゲーム名・作品名など）にはrubyを付けない。付けるときは元の文字列全体を漢字にしてから読みを振る。実装直後に`<rt></rt>`（空rt）と`<ruby>同じ文字<rt>同じ文字</rt></ruby>`（自己参照ルビ）が無いかgrepで機械チェックする——3作連続（ちそうならべ／地図記号タイムアタック／じだいならべ）で発生した既知の落とし穴
- **状態変化（0に落ちた／しきい値を跨いだ等）の検知は真偽値フラグで書く**：「前フレームの値としきい値を比較する」実装（例：`if (前回値 > 0.5 && 今回値 <= 0)`）は、変化が小刻み（%/秒を60fpsで割るなど）だと、しきい値を跨ぐ瞬間の値を捉え損ねて実質発火しないバグになりやすい（はつでんMAXチャレンジで発見）。「フラグが立っている間に条件を満たしたら1回だけ発火してフラグを倒す」方式にする
- **tweenのsetter内で状態フラグを固定値に無条件セットしない**：経過表示が瞬間的にスナップするバグになる。「浮動小数点インデックスをtween→対応配列をlerpArr()で補間参照」のパターンを使う（授業制作ガイド側の教訓と共通）
- **押しっぱなし系とマッシュ（連打）系を混同しない**：`pointerdown`で継続的に加算するのは「押しっぱなしキープ」型、`pointerdown`イベント発火のたびに1回だけ加算するのは「連打/マッシュ」型。どちらのコアループかで実装が変わるので、着手前に決めた入力方式（1章2）と実装を一致させる

---

## 5. 成果物・記録

- **1ファイルHTML**（オフライン動作）のみ。A4記録シートは作らない（授業ではないため）
- ベストタイム/スコアは `localStorage` にプロフィール名前空間で保存し、再訪の動機にする

---

## 6. QA（授業制作ガイド9章を踏襲、一部除外）

- [ ] JS構文チェック：html拡張子は`node --check`非対応のため、`<script>`ブロックを正規表現で抽出して`new Function()`に渡す方法でチェックする
- [ ] rubyタグの自己参照・空rtタグが無いかgrepで確認する（4.5章）
- [ ] claude-in-chromeでロック画面・タイトル画面・プレイ画面のスクショ確認。**file://はナビゲーション制限で開けないことがあるため、その場合はNode.jsの`http`モジュールで対象フォルダを一時的にローカルサーバー化し`http://localhost:PORT/`経由で開く**（QA後はサーバープロセスを停止する）
- [ ] れべるクリア〜全クリア〜リザルト画面の遷移は、`onLevelClear()`等の進行関数をjavascript_toolで直接呼び出して確認する（数レベル分を手動プレイすると許可プロンプトを消費しすぎるため）。耐久/キープ系は`stepXxx(dt)`のような毎フレーム処理関数を直接ループ呼び出しして境界値（0に落ちる瞬間・しきい値を跨ぐ瞬間）を検証する
- [ ] 横長（1920×950）でもレイアウト崩れがない
- [ ] **未クリア時のロック画面**と**クリア済み時の通常プレイ**の両方を確認
- [ ] コンソールエラーが出ていないか確認する（`read_console_messages`）
- [ ] せんせい用バー・`?step=` 復帰はミニゲームには無いので対象外でよい

---

## 7. 完成後にやること（授業制作ガイド11章との違い）

- [ ] `index.html` に追加：通常の学習カード一覧とは**別枠「ごほうびミニゲーム」**を新設し、そこに追加する（対象本編がクリア済みかで表示を出し分けてもよい）
- [ ] **分野マップ（授業制作ガイド10章）・理科ロードマップ／社会ロードマップへの追加は対象外**（新規単元ではなく既存単元の派生コンテンツのため）
- [ ] memoryを更新：対象の本編作品のprojectメモに「派生ミニゲームあり」を追記＋ミニゲーム自体の設計・実装メモを残す
- [ ] `git add -A && git commit`（push はユーザー確認の上）
