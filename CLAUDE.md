# CLAUDE.md

児童福祉施設の小4〜6女子クラス向け「画面で体験するゲーム型授業」シリーズの制作プロジェクト。

## 新しい会話を始めたら最初に

- `授業制作ガイド.md` を読む。制作手法（設計思想・5つ星構成・画面骨格・数値の本物度・技術パターン・実装後チェックリスト・分野マップ）が集約されている。新規制作・修正の前に必ず参照する。
- 理科の新規単元に着手する時は `理科ロードマップ.md` も読み、次回着手候補と各単元の画面化必然性の評価を確認する。
- memory（過去の設計判断・QA教訓・運用ルール）も参照する。
- **`MEMORY.md`に`status: in_progress`の進行中メモ（下記「実装が未完了のまま会話が終わる場合」参照）が見つかったら、それが最優先**。設計のやり直しやAskUserQuestionの再確認はせず、そのメモに書かれた「次にやること」からそのまま続きに着手する（前回合意内容が古くなっていないか一言確認する程度でよい）。**ごほうびミニゲーム制作についても同様の仕組みがある**（`ミニゲーム制作ガイド.md`1.5章参照）。この場合は`project-minigame-series.md`内に見出し「（実装中）」の付いたセクションが無いか確認し、見つかったらそこから続きに着手する。**脳トレコーナー制作についても同様の仕組みがある**（`脳トレコーナー制作ガイド.md`1.5章参照）。この場合は`project-braintrain-series.md`内に見出し「（実装中）」の付いたセクションが無いか確認し、見つかったらそこから続きに着手する。
- **「ふりかえりタイム」（動画で振り返る）を既存作品へ遡及適用中**。ユーザーから「ふりかえりタイムの続き」「次の作品お願い」等の依頼があったら、まず memory の `feature-furikaeri-video.md` を読み、①適用済み作品数、②着手順ルール（完成が古い順に理科⇔社会を交互、ペースは1会話1作品）、③「次回対象」に書かれた作品でそのまま着手する（都度どの作品からやるか聞き直さない）。ガイド「4.5 ふりかえりタイム」章に技術仕様あり。

## 実装が未完了のまま会話が終わる場合（作業再開の引き継ぎ）

新規単元は世界観・ミッション構成をユーザーと合意してから実装に入るが、実装・QAが終わる前に会話が終わる（ユーザーが`/clear`する等）ことがある。**通常の「11. 作品完成後にやること」は完成後にしか動かないため、未完了のまま会話が終わるとその引き継ぎができない。** これを防ぐため、実装中は以下を行う：

- 設計がユーザーと合意できた時点、および区切りのよいタイミング（ミッションを1つ実装し終えた時、QAの節目など）で、`memory`にその作品の進行中メモ（ファイル名は完成後と同じ`project-tsukiyomido-lesson-NN.md`、frontmatterに`status: in_progress`を追加）を作成・更新する。内容は最低限：①単元名・タイトル・ファイル名（slug）、②確定済みの設計（世界観・案内役・5つ星ミッション構成・主要な数値と出典）、③現在の進捗（どこまで実装・QA済みか）、④次にやること、を書く。
- `MEMORY.md`にもその作品の1行索引を追加する（完成後の索引行と同じ形式でよい、進捗が分かる一言を添える）。
- 作品が完成したら、通常どおり「11. 作品完成後にやること」チェックリストを実行し、この進行中メモを完成版の記録に書き換える（`status: in_progress`を削除し、実装・QAの詳細を書き足す）。新しいファイルを別に作る必要はない。

## 1作品が完成したら（実装・QA・コミットが終わったら）必ず行うこと

`授業制作ガイド.md` の **「11. 作品完成後にやること（記録・公開チェックリスト）」** に従う。index.html へのカード追加、分野マップ（ガイド10章）への1行追加、理科ロードマップの更新、memory の追記、git commit（push はユーザー確認の上）を毎回行う。

ここを飛ばすと、次の会話が「もう作ったか」「次は何を作るべきか」をゼロから調べ直すことになりコストが増える。**実装が完了した時点で、指示を待たずにこのチェックリストを実行すること。**

**「ふりかえりタイム」を1作品に追加し終えたら**、上記チェックリストに加えて `feature-furikaeri-video.md` の「適用状況・運用方針」を更新すること：①適用済み作品数を+1、②今回使った動画（あれば）を一覧に追記、③「次回対象」を理科⇔社会交互の次の1件に繰り上げる。これも指示を待たずに実行する。
