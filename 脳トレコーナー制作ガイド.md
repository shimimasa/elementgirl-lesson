# 脳トレコーナー制作ガイド

`授業制作ガイド.md`で作る本編（学習ゲーム型授業）、`ミニゲーム制作ガイド.md`で作る「ごほうびミニゲーム」（本編クリアで解放）とは別に、**教科内容・進捗と無関係にいつでも遊べる認知トレーニング（脳トレ）**を作るための手引き。1作目「ピカッとじゅんばんメモリー」（hikarijun）で確立。

技術的な土台（音・canvas・キラキラ演出などのコード資産）は`授業制作ガイド.md`・`ミニゲーム制作ガイド.md`と完全に共通。このガイドは「アンロックを付けない・元本編と紐付けない」という**差分だけ**を定義する。

---

## 0. 位置づけ・品質基準

- 授業制作ガイド2章の5つ星フォーマット（intro→vote→ミッション×4→はかせチャレンジ→ふりかえりタイム→ending→free）は**使わない**。ミニゲーム同様、完全にゲームとして完結させる
- **アンロック無し**：本編のクリア状況と無関係に、index.htmlを開けば誰でもすぐ遊べる
- **元本編との紐付け無し**：ごほうびミニゲームと違い、特定の単元の復習という位置づけではない。目的は記憶・注意・処理速度・視空間認知といった**認知トレーニングそのもの**
- 品質基準は他シリーズと同じ「夢中になる」体験。ハイスコア/ベスト記録を伸ばしたくなる再訪動機を最優先に設計する

---

## 1. 着手前に決めること

1. **ジャンル**（瞬間記憶／数字ならべ／色と文字の一致判定／図形さがし、の4種を初弾候補としてAskUserQuestionで確定済み。管理は`project-braintrain-series.md`（memory）で行う。追加ジャンルを検討する際は同様にAskUserQuestionで候補を確認する）
2. **コアループ・入力方式**（本シリーズ内で既出パターンと重複しないか確認する。一覧は`project-braintrain-series.md`で管理）
3. **難易度階段**：学習単元の実測値のようなアンカーが無いため、**実在する認知課題（心理学・神経心理学で使われる標準的な課題形式）のルールを土台にする**。架空の数値を事実であるかのように画面へ表示しない（例：「これは平均的な記憶力です」等の断定は避ける）
4. **演出密度**：ごほうびミニゲームと同水準のキラキラ演出・ベスト記録更新のひとことを踏襲する（[[feedback-hoshitsunagi-praise]]参照）
5. **数値バランスの事前検証**：連打・キープ・反応速度など、入力の速さ・継続時間が勝敗を左右するコアループの場合は、ミニゲームガイド1章5と同じくNode.jsでの簡易シミュレーションを実装前に行う
6. 合意した設計方針を`project-braintrain-series.md`に書き残す

---

## 1.5 実装が未完了のまま会話が終わる場合（作業再開の引き継ぎ）

ジャンル・コアループの合意から実装・QA完了までを1回の会話で終えられないことがある。ミニゲームガイド1.5章と同じ考え方をここにも適用する：

- コアループが確定した時点、および区切りのよいタイミング（数値バランス検証完了時、実装完了時、QA完了時など）で、`project-braintrain-series.md`の該当作品セクション（無ければ新規作成）を更新する。セクション見出しに**「（実装中）」**を付け、内容は最低限：①ジャンル・タイトル・ファイル名（slug）、②確定済みの設計（コアループ・入力パターン・難易度階段・参考にした実在の課題形式）、③現在の進捗、④次にやること、を書く
- `MEMORY.md`の脳トレコーナー索引行にも進捗が分かる一言を反映する
- 作品が完成したら、通常どおり「7. 完成後にやること」チェックリストを実行し、進行中セクションを完成版の記録に書き換える（見出しの「（実装中）」を削除）
- 新しい会話で制作を再開する時は、`project-braintrain-series.md`に「（実装中）」の見出しが無いか先に確認する

---

## 2. アンロック機構は無い

`isUnlocked()`・ロック画面・`requireSlug`系のコードは一切書かない。かわりにベスト記録を`profileNamespacedKey()`でプロフィール別に`localStorage`保存する（授業・ミニゲームと同じ名前空間化パターン、`profile.js`を読み込むことで使える）。

```js
function nsKey(base){
  try { return (typeof profileNamespacedKey === 'function') ? profileNamespacedKey(base) : base; }
  catch(e){ return base; }
}
const BEST_KEY = 'xxxxx_best'; // ゲームごとに固有のキー名にする
```

- `auth.js`・`orientation-guard.js`・`profile.js`・`progress.js`は他作品と同じ4本を`<head>`でそのまま読み込む（`progress.js`はバッジ記録が無いため実質使わないが、他ページとの読み込み構成を揃えるため残す）

---

## 3. 画面骨格

```
┌─ header：ロゴ ＋ れべる/ベスト表示 ──────────────┐
├─ main：単一の大きなステージ canvas（操作の主舞台）──┤
│    タップ/クリックで進行                            │
└─ リザルトオーバーレイ（ゲーム終了時）────────────────┘
```

- 丸窓・案内役キャラ・おねがいカード・選択肢ボタン列・せんせい用バー・`?step=`途中復帰・**ロック画面**は不要
- ミニゲームよりもさらにシンプルな骨格になる（アンロック判定分岐が無いため）

---

## 4. 技術資産（授業制作ガイド6章をそのまま流用、新規に書き直さない）

- **音**：`tone()` + `ac()`、効果音（sTap/sYes/sFanfare 等）
- **canvas**：`fitCanvas()`（dpr対応）／`mapTransform()`（`PAD_T`で上部帯を確保）
- **キラキラ**：`burst()` / `drawFx()`（星形パーティクル）
- **CSS**：`clamp()`と vh/vw でレスポンシブ
- コピー元は既存のミニゲーム作品（`ashitouch.html`等）にする。同じ資産をそのまま使い回せる

---

## 4.5 実装時の定番の落とし穴（クオリティを落とす既知パターン）

ミニゲーム制作ガイド4.5章と同じ既知パターンに注意する：

- **rubyタグの自己参照・空rtタグ**：固有名詞（ゲーム名など）にはrubyを付けない。実装直後に`<rt></rt>`と自己参照ルビが無いかgrepで機械チェックする
- **状態変化（しきい値を跨いだ等）の検知は真偽値フラグで書く**：前フレーム値との単純比較は取りこぼしバグになりやすい
- **tweenのsetter内で状態フラグを固定値に無条件セットしない**：経過表示が瞬間スナップするバグになる
- **押しっぱなし系とマッシュ（連打）系を混同しない**：着手前に決めた入力方式（1章2）と実装を一致させる

---

## 5. 成果物・記録

- **1ファイルHTML**（オフライン動作）のみ。A4記録シートは作らない
- ベストスコア/ベストタイムは`localStorage`にプロフィール名前空間で保存し、再訪の動機にする

---

## 6. QA（ミニゲームガイド6章を踏襲、ロック関連は対象外）

- [ ] JS構文チェック：`<script>`ブロックを正規表現で抽出して`new Function()`に渡す方法でチェックする
- [ ] rubyタグの自己参照・空rtタグが無いかgrepで確認する（4.5章）
- [ ] ユーザー向けテキスト（タイトル・説明文・ボタン・結果表示）に漢字が紛れ込んでいないか、`Grep`で`[一-龯]`を検索して機械確認する（本シリーズはるび機構を持たず全文ひらがな・カタカナで統一する運用のため、目視だけでは見落としやすい。JSコメント内の漢字は対象外）
- [ ] claude-in-chromeでタイトル画面・プレイ画面のスクショ確認。file://が開けない場合はNode.jsの`http`モジュールで一時的にローカルサーバー化する
- [ ] れべる進行〜終了〜リザルト画面の遷移は、進行関数をjavascript_toolで直接呼び出して確認する（許可プロンプト消費を抑えるため）
- [ ] 横長（1920×950）でもレイアウト崩れがない
- [ ] コンソールエラーが出ていないか確認する（`read_console_messages`）
- [ ] **ロック画面・せんせい用バー・`?step=`復帰の確認は対象外**（このシリーズには存在しない）

---

## 7. 完成後にやること

- [ ] `index.html`に追加：通常の学習カード一覧・ごほうびミニゲームとは**別枠「🧠 あたまのたいそう」**を新設し、そこに追加する
- [ ] **分野マップ（授業制作ガイド10章）・理科ロードマップ／社会ロードマップへの追加は対象外**（学習単元ではないため）
- [ ] memoryを更新：`project-braintrain-series.md`に設計・実装メモを残し、`MEMORY.md`の索引を更新
- [ ] `git add -A && git commit`（pushはユーザー確認の上）
